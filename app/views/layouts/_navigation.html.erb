<nav class="navbar nnavbar-inverse">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "BLOGBUSTERS", welcome_path, class: "navbar-brand", id: "logo" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
      <% if current_user %>
        <li><%= link_to "Members", users_path %>
        <li><%= link_to "Articles", articles_path %></li>
        <li><%= link_to "Create Article", new_article_path %></li>
        <p class="navbar-text">Signed in as <%= current_user.username %></p>
        <% end %>
          </ul>
        </li>
      </ul>
      
      <ul class="nav navbar-nav navbar-right">
      <% if current_user %>
      <button type="button" class="btn btn-warning navbar-btn"><%= link_to "Log Out", sessions_path, method: :delete %> </button>
      <% else %>
      <button type="button" class="btn btn-success navbar-btn"><%= link_to "Sign in", new_session_path %> </button>
      <button type="button" class="btn btn-warning navbar-btn"><%= link_to "Sign Up", new_user_path %> </button>
      <% end %>
        
        
          </ul>
        </li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>